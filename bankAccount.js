class BankAccount {
  constructor() {
    this.INITIAL_BALANCE = 0;
    this.balance = this.INITIAL_BALANCE;
    this.transactions = [];
  }

  deposit(amount, date) {
    
  }
}

module.exports = BankAccount;